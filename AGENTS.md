# AGENTS.md

このファイルは、湯間庭町のリポジトリを扱う生成AI・コーディングエージェント向けの運用ガイドです。

## 1. このリポジトリの目的

湯間庭町は、個人制作したゲーム、触れるらくがき、記事などを、ひとつの架空の温泉町の中で歩いて見つけられるWebサイトです。

公開URL:
https://sukimastock.github.io/yumaniwa-town/

`README.md` は一般の訪問者向けです。開発手順、内部構造、デバッグ方法などを `README.md` に追加しないでください。

## 2. 基本方針

- 既存の世界観、URL、ID、データ構造をできるだけ維持する。
- 指示されていない大規模リファクタやファイル移動をしない。
- 通常のコンテンツ追加では `main.js` を変更しない。
- 既存アセットを、明示的な指示なしに削除・置換しない。
- 変更は必要最小限にし、既存作品への導線を壊さない。
- 公開ページに開発用UIやデバッグ表示を残さない。
- SEO・OGP・ホーム画面追加用のメタ情報を変更する場合は、既存設定を確認してから行う。

## 3. 日常運用で触る場所

### note記事を追加する

`data/notes.js` の次のマーカー直後に、新しい記事を追加する。

```js
// [NOTES:ADD_NEWEST_HERE]
```

### ゲーム・触れるらくがきを追加する

`data/works.js` の次のマーカー直後に追加する。

```js
// [WORKS:ADD_NEWEST_HERE]
```

主な値:

- `venue: "leisure_center"` → 湯窓レジャーセンター
- `venue: "tomogushi_alley"` → 灯串横丁
- `status: "open"` → 公開中
- `status: "preparing"` → 制作中。町の通常メニューには出さない
- `status: "hidden"` → 町には表示しない
- `launch: "embedded"` → 町の共通プレイヤー内で開く
- `launch: "external"` → 外部URLを開く

### 更新履歴を追加する

`data/updates.js` の次のマーカー直後に追加する。

```js
// [UPDATES:ADD_NEWEST_HERE]
```

ここへ追加した内容は、町の更新履歴に反映される。

### 施設の説明やメニューを変更する

`data/places.js` を確認する。

### マップ・当たり判定・入口を変更する

`data/station-plaza.js` など、対象エリアのデータファイルを確認する。

## 4. `main.js` を触る場合

`main.js` は町の本体ロジックです。

通常の記事追加、作品追加、更新履歴追加では変更しないでください。

次のような場合のみ変更対象になります。

- 町そのものの挙動を追加・修正する
- 共通UIを変更する
- 移動、調べる、作品プレイヤーなどの仕組みを変更する
- 新しいデータ形式を導入する

変更前に、データファイルだけで対応できないか確認してください。

## 5. 開発モード

`main.js` の先頭付近にある開発モード設定を使用する。

```js
var DEV_MODE_ENABLED = false;
```

公開状態では必ず `false` にする。

`true` にすると、開発ボタンや開発用ショートカットが有効になる。
明示的に依頼されない限り、`true` のまま公開ブランチへ残さないこと。

## 6. 触れるらくがき・共通プレイヤー

レジャーセンターの作品は、町の上に開く共通プレイヤーで表示する。
作品本体は iframe 内で独立して動く。

主な場所:

- `engine/rakugaki-engine.v1.js` → 触れるらくがき用エンジン
- `engine/README.md` → エンジン固有の説明
- `works/_template/` → 新しい作品を移植するときの雛形
- `works/<作品ID>/` → 各作品
- `data/works.js` → 作品の公開状態、設置場所、起動方法

埋め込み作品の例:

```js
{
    id: "rainy-window",
    title: "雨の日の窓",
    venue: "leisure_center",
    kind: "work",
    status: "open",
    launch: "embedded",
    entry: "./works/rainy-window/index.html",
    emptyText: "この筐体は現在調整中です。"
}
```

外部作品は `launch: "external"` と `url` を使用する。

## 7. 座標ルール

Rakugaki Engine の作品座標は Codea 式。

- 左下が `(0, 0)`
- Yは上向き

ブラウザCanvasを使う作品では、`engine/README.md` の `createCodeaLayer()` を使用し、作品側で独自のY反転処理を重ねないこと。

## 8. SEO・公開ページのルール

`index.html` には検索・SNS共有用の設定がある。

特に以下を不用意に削除・変更しない。

- `<title>`
- `meta name="description"`
- `rel="canonical"`
- OGP (`og:*`)
- Twitter/X Card (`twitter:*`)

canonical は湯間庭町の公開URLを指す状態を維持する。

`README.md`、ページタイトル、description、OGP説明などの一般公開テキストに、内部の更新手順、デバッグ方法、パッチ手順などを混ぜない。

## 9. 変更時の確認

変更後は、該当範囲に応じて次を確認する。

- 町が起動する
- 駅前広場で操作できる
- 既存の移動先が壊れていない
- 「調べる」が使える
- 作品プレイヤーが開いて閉じられる
- 新しい記事・作品・更新履歴が意図した場所に出る
- 公開状態で開発UIが見えていない
- 既存の公開URLが変わっていない

## 10. AIへの重要な指示

作業を始める前に、対象ファイルと既存実装を読むこと。

ユーザーから「追加」「修正」など限定的な依頼を受けた場合、関係のない箇所まで整理・最適化しないこと。

不明点があっても、既存コードから安全に判断できる範囲では既存方式に合わせること。

このファイルの内容と実際のコードが食い違う場合は、実際のコードを優先し、必要であればこのファイルも更新する。
